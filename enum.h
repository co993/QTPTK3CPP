#ifndef ENUM_H
#define ENUM_H

#include <QWidget>

class enum : public QWidget
{
    Q_OBJECT
public:
    explicit enum(QWidget *parent = nullptr);
    -std=c++14 const Q_DECL_ASSOCIATIVE_FORWARD_ITERATOR();
   -std=c++98 const Q_DECLARE_ASSOCIATIVE_FORWARD_ITERATOR();

signals:

};

#endif // ENUM_H
